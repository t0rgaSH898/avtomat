# Примеры проверки работы LBA

## Пример 1. Строка `abc` ($n=1$)

**Ожидаемый результат:** принята.

**Трассировка:**
1. Начало: $q_0$, лента: `abc☐`.
2. $q_0 \xrightarrow{a \to X, R} q_1$: лента `Xbc☐`.
3. $q_1 \xrightarrow{b \to Y, R} q_2$: лента `XYc☐`.
4. $q_2 \xrightarrow{c \to Z, L} q_3$: лента `XYZ☐`.
5. $q_3 \xrightarrow{Y \to Y, L} q_3$: позиция на $Y$.
6. $q_3 \xrightarrow{X \to X, R} q_0$: возврат к началу.
7. $q_0 \xrightarrow{Y \to Y, R} q_4$: пропуск $Y$.
8. $q_4 \xrightarrow{Z \to Z, R} q_4$: пропуск $Z$.
9. $q_4 \xrightarrow{\sqcup \to \sqcup, —} q_{accept}$.

**Результат:** принята.

---

## Пример 2. Строка `aabbcc` ($n=2$)

**Ожидаемый результат:** принята.

**Трассировка:**
1. Первый проход: `aabbcc☐` → `Xaabbcc☐` → `XXabbcc☐` (обработка `a`).
2. `XXabbcc☐` → `XXYYbcc☐` → `XXYYZZc☐` (обработка `b` и `c`).
3. Возврат к началу: $q_3 \xrightarrow{X \to X, R} q_0$.
4. Переход в $q_4$, проверка $Y$ и $Z$: `XXYYZZ☐`.
5. Достижение $\sqcup$ → $q_{accept}$.

**Результат:** принята.

---

## Пример 3. Строка `aabcc` (ошибка: $n_a = 2$, $n_b = 2$, $n_c = 2$ — но структура нарушена)

**Ожидаемый результат:** отклонена.

**Трассировка:**
1. После обработки первых двух `a` и двух `b`: лента `XXYYcc☐`.
2. При попытке заменить `c` на $Z$ и вернуться, автомат не найдёт соответствие между количеством $X$, $Y$, $Z$.
3. В какой‑то момент головка встретит символ, не предусмотренный правилами (например, `c` после $Z$).
4. Переход в $q_{reject}$.

**Результат:** отклонена.

---

## Пример 4. Строка `ab` (недостаточное количество `c`)

**Ожидаемый результат:** отклонена.

**Трассировка:**
1. Обработка `a` → $X$, `b` → $Y$: лента `XY☐`.
2. Головка достигает $\sqcup$, но не все символы обработаны.
3. Невозможность выполнить переход по правилам
